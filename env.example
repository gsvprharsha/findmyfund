# Add the following environment variables to your .env file while in development

# Database URL with connection pooling parameters
# Add these parameters to prevent connection flooding:
# ?connection_limit=5&pool_timeout=10
# Example: postgresql://user:pass@host:5432/db?connection_limit=5&pool_timeout=10
DATABASE_URL=""
